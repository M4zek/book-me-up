
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/book_me_up?serverTimezone=UTC
    username: admin_db
    password: admin_password_db
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect