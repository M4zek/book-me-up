# Database information
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/book_me_up_test?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
